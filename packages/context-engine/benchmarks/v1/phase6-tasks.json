{
  "schemaVersion": "continuum.agent-effectiveness-task-set.v1",
  "groundTruthPolicy": "Task references and required evidence are frozen before agent execution; Continuum output never defines ground truth.",
  "tasks": [
    { "id": "small-local-cache-timeout", "source": "cases.json", "family": "local_bug", "validation": "fixture_tests", "hiddenValidation": "required", "acceptanceCriteria": ["readCache(\"timeout\") must remain an explicit cache miss and return undefined, not a stale value.", "cacheTimeoutTest must directly assert the timeout cache-miss behavior."] },
    { "id": "small-local-token-refresh", "source": "cases.json", "family": "local_bug", "validation": "fixture_tests", "hiddenValidation": "required", "acceptanceCriteria": ["refreshToken must use the same empty/expired-token validation as validateToken.", "Add a regression test proving an invalid refresh token is rejected."] },
    { "id": "mono-cross-user-session", "source": "cases.json", "family": "cross_package", "validation": "fixture_tests", "hiddenValidation": "required", "acceptanceCriteria": ["updateUser(id) must return the input id and a sessionId created from that same id.", "createSession must preserve the userId, and the route test must assert both id and sessionId."] },
    { "id": "mono-cross-web-contract", "source": "cases.json", "family": "cross_package", "validation": "fixture_tests", "hiddenValidation": "required", "acceptanceCriteria": ["fetchUser and UserResponse must agree on the complete response shape.", "Preserve the sessionId contract across the web client and API contract."] },
    { "id": "sql-migration-users", "source": "cases.json", "family": "database_migration", "validation": "fixture_tests", "hiddenValidation": "required", "acceptanceCriteria": ["The users migration must create the documented id/email schema and provide a reversible rollback.", "The migration test must cover apply and rollback, and schema documentation must match."] },
    { "id": "sql-migration-refresh", "source": "cases.json", "family": "database_migration", "validation": "fixture_tests", "hiddenValidation": "required", "acceptanceCriteria": ["Use refresh_token_hash, index that hash column, and document a rollback that preserves existing rows.", "Add migration-test coverage for the refresh-token migration and rollback."] },
    { "id": "small-api-contract", "source": "cases.json", "family": "api_contract", "validation": "fixture_tests", "hiddenValidation": "required", "acceptanceCriteria": ["Keep UserApi and UserController.getUser return types consistent.", "The controller must return the requested user id without weakening the public contract."] },
    { "id": "mono-api-contract", "source": "cases.json", "family": "api_contract", "validation": "fixture_tests", "hiddenValidation": "required", "acceptanceCriteria": ["Keep UserResponse, updateUser, and its route test consistent.", "The response must include the documented id and sessionId fields."] },
    { "id": "small-security-token", "source": "cases.json", "family": "security", "validation": "fixture_tests", "hiddenValidation": "required", "acceptanceCriteria": ["Reject empty and expired credentials in validateToken.", "refreshToken must delegate to validateToken and never weaken the policy.", "Add tests for empty and expired credentials and document the policy in SECURITY.md."] },
    { "id": "sql-security-refresh", "source": "cases.json", "family": "security", "validation": "fixture_tests", "hiddenValidation": "required", "acceptanceCriteria": ["Store only refresh_token_hash, never plaintext refresh tokens.", "Keep rollback safe and add a migration test proving the hashed-token schema.", "Document the plaintext-token prohibition in SECURITY.md."] },
    { "id": "small-config-timeout", "source": "cases.json", "family": "configuration", "validation": "fixture_tests", "hiddenValidation": "required", "acceptanceCriteria": ["config/app.yaml and loadAppConfig must agree on timeoutMs.", "Keep the configuration loader type-safe and add or update its timeout test."] },
    { "id": "small-doc-mismatch", "source": "cases.json", "family": "documentation_mismatch", "validation": "fixture_tests", "hiddenValidation": "required", "acceptanceCriteria": ["Document the actual AuthService validation and timeout behavior.", "Keep authentication documentation, implementation, and configuration consistent."] }
  ]
}
